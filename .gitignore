# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/
*.whl
*.tar.gz

# Testing / tooling
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/
ENV/

# Environment / secrets
.env
.env.local

# Editor state
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# KiCad autosave / backup
*-bak/
*.kicad_*.bak
*.kicad_pcb-bak
*.kicad_sch-bak
_autosave-*

# Local session state (per-project, not shared)
.kicad_jlcpcb_session.json

# Local Claude Code user settings (per-machine, not shared)
.claude/


# Eval run artifacts (scores are reproducible; the HTML reports are large)
evals/results/

# uv writes this when the launcher bootstraps dependencies. Deliberately not
# committed: the whole point of the uv path is to resolve within the bounds
# pyproject declares, and a shipped lock would freeze every user on whatever
# versions happened to be current here. The load-bearing constraint (mcp<2)
# lives in pyproject.toml, which is where it belongs.
uv.lock
